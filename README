# Exercise 1 — Data Preprocessing & Regression

This exercise focuses on data preprocessing techniques and regression models using Python.

The project includes preparing and cleaning the dataset, transforming the data into a suitable format, and applying regression methods to analyze and predict the target variable.

The implementation is provided in a Jupyter Notebook.
